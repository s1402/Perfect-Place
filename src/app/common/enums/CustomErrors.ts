export enum CustomError {
    SERVER_IS_DOWN = "Server Is Down",
    INVALID_FILE_UPLOAD = "Upload one file first",
    UPLOAD_AT_LEAST_ONE_IMAGE = "Upload at least one image",
    FILE_TOO_LARGE = "File is too large. Please select a file under 5MB."
}